[package]
name = "foxytunnel-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true

[lints]
workspace = true

[dependencies]
arti-client = { version = "0.42.0", default-features = false, features = ["tokio", "rustls", "static-sqlite"] }
rustls = { version = "0.23.40", default-features = false, features = ["ring"] }
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1.52.3", default-features = false, features = ["io-util", "net", "rt", "sync"] }
toml = "1.1.2"
tor-rtcompat = { version = "0.42.0", default-features = false, features = ["tokio", "rustls"] }
